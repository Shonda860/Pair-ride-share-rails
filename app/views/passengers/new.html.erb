<h1>Add New Passenger</h1>
	<%= form_with model: @passenger, class: "create_passenger" do |f| %>
		<h4>Please provide the following information to register a new passenger:</h4>

		<%= f.label :name %>
		<%= f.text_field :name %>

		<%= f.label :phone %>
		<%= f.text_field :phone_num %>

		<%= f.submit "Save Passenger", class: "passenger-button" %>

	<% end %>

